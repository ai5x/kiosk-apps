# Kiosk-Apps Local Configuration
# This file contains sensitive credentials and is NOT tracked in git
# Copy this to .env.local and fill in the values

# GitHub Personal Access Token (PAT)
# Read-only token for accessing private kiosk-apps repository
# Required for auto-updates from private repos
# Generate at: https://github.com/settings/tokens
# Permissions needed: repo (read-only)
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# This token should be the same read-only PAT used in cluster-apps
# for consistency and security
